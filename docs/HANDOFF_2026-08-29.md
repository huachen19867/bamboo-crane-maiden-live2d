# 竹鹤少女 Live2D 暂停与研究交接（2026-08-29，2026-08-30 三次更新）

> 2026-08-30 验收更正：V3 的“眨眼已落地”结论撤回。重新打开同一 CMO3 后，把左、右眼开闭均拨到 `0`，画面仍显示睁眼；API 可列出的 `0 / 0.5 / 1` 键槽不等于渲染绑定成功。本轮产生的失败尝试已隔离在 `tmp/cubism-v4-acceptance-preserve/bamboo-crane-maiden-v4-head-production-v3-import-failed-blink-key-attempt-E1A42B3B.cmo3`（SHA `E1A42B3B…`），不得交付。正式路径已恢复为 Git 可追溯的 SHA `53F5D8E9…76FD`。`exports/authoring-archive/2026-08-30-blink-debug/` 保存了 51 个调试产物，均不是验收证据。

> 历史记录（2026-08-30 深夜，**已被上述复测推翻，不可作为成果或证据**）：当时曾依据 API 键槽和调试截图，把 V3 记为“眨眼差分第一版已落地”。脸底烘焙眼睛的资产根因确已在构建脚本 v3 修复并重新导入：`model/cubism-v4/bamboo-crane-maiden-v4-head-production-v3.psd`（SHA `5844EA59…335`）。但 `...v3-import.cmo3` 中存在 `0 / 0.5 / 1` 键槽，不能证明参数 0 时闭眼渲染成立；此前 `exports/v4-blink-state-*.png` 均已归档为调试产物。上眼睑网格键形仍未完成，半闭态也未验收。

## 当前接手点（最新）

头部生产脚本为 `pipeline/v4/build_head_production_psd.py`（现为 v3：脸底眼部背景板修复），生产候选 PSD 为 `model/cubism-v4/bamboo-crane-maiden-v4-head-production-v3.psd`（SHA-256 `5844EA59E1BF3AB1E99252CADCFC487FF16E7A6032C39E79007281CEC53A8335`），配套预览 `exports/v4-head-production-neutral-v3.png`、报告 `exports/v4-head-production-report-v3.json`。V2 的 PSD/预览/报告保留作上一版对照。

导入检查点为 `model/cubism-v4/bamboo-crane-maiden-v4-head-production-v3-import.cmo3`（SHA-256 `53F5D8E94B2A9AE34CAFAF3EC054750BF04D46666A641ABB8D4BC0E39CC76BFD`，参数 1/1 中性）：33 个 ArtMesh、778 网格顶点。双眼曾以 API 写入 `0 / 0.5 / 1` 的透明度键槽，但保存、重开后从 GUI 把参数拨到 0 仍睁眼，故所有 API 键位报告、闭眼截图和 `v4-reopen-check.json` 都仅作失败复现材料，绝非验收证据。外部 API 对结构和属性的语义实测仍可参考 `docs/TECH_LOG.md` 与 `pipeline/v4/cubism_api_probe.mjs`、`pipeline/v4/native_input.py`，但必须以 Editor 的实际画面复核。

V1 (`...head-production-v1.psd` 及 `...-v1.png`) 只保留作失败对照，原因是洋红环、秃额刘海、眉 ROI 误抓发丝、颈底矩形；不要导入或继续在 V1 上修。V2 的面部候选仍来自 `assets/source/rebuild-v4/head-production/face-base-chroma-v2.png`，这是生成式底绘候选，不是原始身份真值；若新模型要求更高身份一致性，应替换该候选并重新运行脚本，而不是在 Cubism 里涂改成不可追溯的贴片。

Git 交接状态（2026-08-30）：V2 脚本、素材、PSD、预览和报告，以及 V3 的中性导入检查点、探针和验收脚本均已提交。注意，过往提交中的“眨眼验收”措辞已被本文件与技术日志的 2026-08-30 复测条目撤回；接手模型应以当前 CMO3 的 SHA 和 GUI 复测为准。不要清理 V1 失败证据、V3 中性导入点或已归档的失败调试材料。

## 当前裁决

老板已紧急叫停旧制作。旧网页原型和 Cubism MVP 的“会动”不等于可用：人物拼接感严重、眼睛比例失真、运动像贴片绕轴旋转。进一步审计确认，原始场景图的人物有效分辨率、姿态和遮挡条件不足以支撑商业级全身关节重建。不要继续在旧模型上加动作、Physics 或导出包。

下一位 AI 的第一份必读材料是 `docs/REFERENCE_PROJECTS_RESEARCH_2026-08-29.md`，其次是 `docs/USER_ACCEPTANCE_SPEC.md` 和 `docs/TECH_LOG.md`。六个参考项目源码已在 `references/live2d-research/`，下载清单与哈希在该目录 README。

## 保留的权威材料

原始身份/服装参考是 `assets/source/reference.png`，SHA-256 `C947AF32EA42A0D2AA3BBBC3C276003FED80153F2F00EFDDDD7C2CF1F711F67D`。`assets/source/generated-extraction.png` 只用于说明旧透明提取的清晰度和边缘问题，不是新母版。

旧路线只保留两个工作点：

| 文件 | 用途 | SHA-256 |
|---|---|---|
| `model/cubism/bamboo-crane-maiden-arm-rebuild-v3-seamfix-v1.cmo3` | 旧路线最后一个 Editor 回退点，只作层级/参数参考 | `27C9F08ED548175F4A8931B0E22729548D9DD1A1351C1E28E3CBE5B10B4AEA40` |
| `model/cubism/bamboo-crane-maiden-arm-rebuild-v3-seamfix-v1.psd` | 对应旧 PSD | `0DDA966725175825602A2FFC552EEAF15E140A95525CEDCA9070A1416ECFA2F6` |
| `model/cubism/bamboo-crane-maiden-arm-rebuild-v3.cmo3` | seam-fix 前回退 | `1DBD5EB7D9CA9BD42C9CF3F0F95BFFA96F55A6166299679ED5F58D98918BF8A5` |
| `model/cubism/bamboo-crane-maiden-arm-rebuild-v3.psd` | 对应 seam-fix 前 PSD | `DA549BA6EEDA1B1456B5F4F268B546A2BD47A2A9F0A3C4F98E2B6F5AAD9C10FE` |

`model/cubism/runtime-arm-v3/` 仅作旧导出格式参考，不代表 seam-fix 已导出或通过视觉验收。保留的外观证据是 `exports/cubism-arm-rebuild-v3-seamfix-preview.png`、`exports/cubism-v3-seamfix-existing-elbow-sequence-contact.png`、`exports/cubism-v3-seamfix-existing-wrist-sequence-contact.png` 及对应 JSON 报告。Anime2.5DRig 自带样例运行证据在 `exports/reference-research/`。

## 清洗结果与恢复边界

本轮删除的是可再生成的下载 ZIP、Node 依赖和构建目录、临时截图/脚本、Cubism 逐步操作截图、错误的另一角色底绘、低质量候选图、旧网页/MOC 生成物、失败的历史 CMO3/PSD 检查点、旧交付压缩包，以及依赖这些已删除素材的旧 Viewer、启动器和构建/验收脚本。已安装的 Cubism 5.4 alpha 程序保留，只删除安装包；受控 Python 运行时也保留。

按最终递归统计，工作文件约 0.93GB；计入本地 Git 历史后，整个目录由清洗前约 4.38GB 降到约 1.42GB，释放约 2.96GB。标准 `git gc` 后 `.git` 约 484MB，没有重写历史。

研究和清洗已记录为本地 Git 提交，最终哈希以 `git log -1` 为准。本轮没有推送远端，不能表述为“GitHub 已更新”。

被 Git 跟踪过的旧文件可从仓库历史恢复；未跟踪的临时截图、下载 ZIP和历史检查点属于直接删除，不保证可恢复。原图、最新两个 V3 工作点、研究源码和关键研究证据在删除前均按绝对路径核对。

## 下一阶段正确起点

打开 `model/cubism-v4/bamboo-crane-maiden-v4-head-production-v3-import.cmo3`（中性参数均为 1）。下一道门是从 PSD 逐层核对可见睫毛、眼睑、眼白、虹膜、瞳孔、高光的真实来源，再在 Editor 内先完成一只眼的真实闭合：参数 0 时眼白、虹膜、瞳孔、高光全部消失，上下眼睑连续闭合；参数 0.5 为自然半闭。必须保存、关闭、重开并在无网格 GUI 画面分别拨 1 / 0.5 / 0 验证；一眼通过后才做另一眼，双眼通过后才处理头部 Warp 和头发。API 只能批量建 Part/Deformer/参数结构、写透明度类属性，不能证明 ArtMesh 顶点、Warp 控制点、轴心、Glue 或 Physics 已正确渲染。右臂肩肘腕底绘未完成前不得建立全身 Rotation。

头部通过后再处理右臂：从 `ArtSleeveR` 现有可见袖面拆肩/上臂/肘/前臂/腕/手，补真实颜色的隐藏底绘，并确保 `ArtBodyBase` 不再保留会移动的整片右袖。右臂底绘未完成前，不得建立肩肘腕 Rotation。全身腿脚、衣袖风场、物理、动作/动捕仍全部未完成。

素材到位后可用 see-through 做多 seed 候选分层，但扩散补绘必须逐层与母版复合审查；再人工补全肩肘腕、腰髋膝踝和眼口差分。用 Anime2.5DRig 或 AutoLive2d 做一次性小动作 PSD lint/预检，过门后才进入官方 Cubism。正式验收顺序固定为中性画面、面部比例、各关节双极值与中间值、连续播放、衣发 Physics、最后才是操演/动捕。

任何人都不能把以下内容写成成果：规则矩形网格等于自然 ArtMesh；see-through 补绘等于真实遮挡背面；EasyVtuber 神经逐帧输出等于 Cubism；MOC 能打开等于模型好看；参考图整帧贴回得到的相似度等于动态模型相似度。
